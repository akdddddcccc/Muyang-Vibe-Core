CORE_HOST=0.0.0.0
CORE_PORT=8787

# Comma-separated browser origins. Never use * in a production environment.
CORS_ORIGIN=https://cmuyang23333.top

# The unit-owned Provider Adapter receives the normalized typography request.
# Keep OpenAI/OFOX keys in that adapter, never in the frontend or this repository.
TYPOGRAPHY_ADAPTER_URL=
TYPOGRAPHY_ADAPTER_TOKEN=
