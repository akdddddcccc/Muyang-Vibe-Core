# Muyang-Vibe-Core

Muyang 正式产品的共享 API、Provider Adapter、桌面与移动端基础。

## 第一阶段边界

- 采用 npm workspaces。
- 建立贴片与 Task Map 的服务边界、共享契约和 Provider Adapter 接口。
- 不在此阶段接入真实模型、数据库、密钥或生产部署。

## 本地检查

```bash
npm install
npm run typecheck
```
